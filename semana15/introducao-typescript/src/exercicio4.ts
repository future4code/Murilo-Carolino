// Exercício 4

// a)
/* Para que o código escrito em ts sejá transpilado, e assim lido e executado em JavaScript, é necessário 
inserir o comando tsc no terminal em seguida com seu nome.ts. */

// b)
/* O processo em si não, porem teria que ao utilizar o comando tsc, atribuir o caminho específico do arquivo ts.
Por exemplo: tsc src/test.ts && node src/test.js */

// c)
/* Utilizando apenas o comando tsc, se consegue transpilar todos os arquivos ts da pasta */

// d)
/* Há varias outras configurações interessantes para qual o projeto seja especificado, como por exemplo "isolatedModules", 
que faz a transpilação de cada modulo separadamente. */